import { HeroSlide } from './heroSection.types';
export declare const useHeroSection: (slides: HeroSlide[]) => {
    current: number;
    moveDot: (index: number) => void;
    hasSlides: boolean;
};
//# sourceMappingURL=useHeroSection.d.ts.map